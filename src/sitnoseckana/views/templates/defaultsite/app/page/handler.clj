(ns sitnoseckana.views.templates.defaultsite.app.page.handler)

(defn handle-index [ctx]
  ; (assoc ctx :page-attr {:id "ASDF" :title "app page index"}) ; load list of pages, media sets, whatever is required by template 
  ctx)